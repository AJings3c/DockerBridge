#!/usr/bin/env sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$ROOT"

: "${DOCKGE_DATA_DIR:=./data}"
: "${DOCKGE_STACKS_DIR:=./stacks}"
export DOCKGE_DATA_DIR DOCKGE_STACKS_DIR

exec "$ROOT/runtime/node" "$ROOT/node_modules/tsx/dist/cli.mjs" "$ROOT/backend/index.ts" "$@"
